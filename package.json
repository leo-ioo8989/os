{
  "name": "leo-os",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:unit": "pnpm --filter @founder-os/core test && pnpm --filter @founder-os/db test && pnpm --filter @founder-os/api test && pnpm --filter @founder-os/worker test",
    "test:db": "pnpm --filter @founder-os/db test",
    "test:integration": "pnpm --filter @founder-os/db test:integration",
    "db:start": "docker compose -f docker-compose.verification.yml up -d postgres",
    "db:stop": "docker compose -f docker-compose.verification.yml down",
    "db:reset": "docker compose -f docker-compose.verification.yml down -v && docker compose -f docker-compose.verification.yml up -d postgres",
    "db:status": "docker compose -f docker-compose.verification.yml ps",
    "db:generate": "pnpm --filter @founder-os/db generate",
    "db:validate": "pnpm --filter @founder-os/db exec prisma validate",
    "db:migrate": "pnpm --filter @founder-os/db migrate:deploy",
    "db:smoke": "pnpm --filter @founder-os/db smoke",
    "verify:v107": "pnpm db:generate && pnpm db:validate && pnpm db:migrate && pnpm typecheck && pnpm lint && pnpm test:unit && pnpm test:integration",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
