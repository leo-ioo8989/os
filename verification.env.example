# Disposable V1.07 verification database only.
# Never use this value for production.
NODE_ENV=test
DATABASE_URL=postgresql://postgres@localhost:55432/founder_os_test

# Keep runtime workers disabled during Phase 1 verification unless a test explicitly needs them.
LEO_OS_WORKER_RUN=false
